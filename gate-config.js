// Single source of truth for the client-side password gate. Flip to true to bring
// it back on the portfolio and on every game page at once — the password code in
// index.html stays intact either way.
window.GATE_ENABLED = false;
